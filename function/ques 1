# write a program in python to check the person is eligible for voting or not.
def vote(x):
    if x>=18:
        print("Eligible to vote.")
    else:
        print("Not eligible to vote.")
x=int(input("Enter age : "))
(vote(x))
