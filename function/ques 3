# define a fuction in python that acepts 3 values and returns the maximum of three number.
def max(a, b, c):
    if a > b and a > c:
        print(f"{a} is maximum among all")
    elif b > a and b > c:
        print(f"{b} is maximum among all")
    else:
        print(f"{c} is maximum among all")
        max(30,22,18)
